{
  "name": "@payback/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx convex dev",
    "deploy": "bunx convex deploy",
    "codegen": "bunx convex dev --once",
    "lint": "eslint \"convex/**/*.ts\" \"tests/**/*.ts\"",
    "lint:fix": "eslint \"convex/**/*.ts\" \"tests/**/*.ts\" --fix",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "echo \"Backend build handled by Convex CLI\"",
    "clean": "rm -rf node_modules .turbo"
  },
  "devDependencies": {
    "@payback/config-eslint": "workspace:*",
    "convex-test": "^0.0.41"
  },
  "dependencies": {
    "convex": "^1.31.3"
  }
}
